import { SharePageClient } from "@/components/psychotype/share-page-client"

// Preview route. The real route will fetch the result by token and pass it in.
export default function SharePage() {
  return <SharePageClient />
}
